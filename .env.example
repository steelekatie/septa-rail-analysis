# Copy this file to .env and fill in real values. .env is gitignored.

# Census Bureau API key, used for the ACS station-area demographic merge
# (see CLAUDE.md's 11-series consolidation note -- Appendix C.5's table is kept
# in the report but no longer traces back to a script in this repo)
# Request one at https://api.census.gov/data/key_signup.html
CENSUS_KEY=
